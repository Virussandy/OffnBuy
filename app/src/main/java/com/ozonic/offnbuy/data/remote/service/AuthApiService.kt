package com.ozonic.offnbuy.data.remote.service

// We don't need a separate API service for Auth as we are using the Firebase SDK directly.
// The SDK handles all the low-level API calls and state management for us.